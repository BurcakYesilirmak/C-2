#include<stdio.h>
int main()
{
int i,ftr,n;
ftr=1;
printf("calculating factorial:\n");
printf("enter the num that you want to calculate\n");
scanf("%d",&n);
if(n!=0) {
 for(i=1;i<=n;i++) {
  ftr*=i;
 }
 printf("%d!= %d\n",n,ftr);
}
else {
printf("0!=1\n");
}
return 0;
}
